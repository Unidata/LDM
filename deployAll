# Deploys LDM7 to all permitted hosts

set -e

versOpt=${1:+-v $1}

cd `dirname $0`

# NOAAPort ingest test system (password required)
#deploy -M /usr/ccs/bin/make -n burns

# Non-operational backend cluster node
# Tom's using uni14 for fanout testing
#deploy uni17

# Lead
#deploy lead

# LDM7 systems (no password required)
./deploy -m              -u ldm7 $versOpt ca >&ca.deplog &
#./deploy -m              -u ldm7 $versOpt co >&co.deplog &
./deploy -m -r centos -s -u ldm7 $versOpt md >&md.deplog &
./deploy -m                      $versOpt mo >&mo.deplog &
./deploy -m                      $versOpt nj >&nj.deplog &
./deploy -m           -s         $versOpt ut >&ut.deplog &
#./deploy -m -r ldm7   -s -u ldm7 $versOpt va >&va.deplog &
./deploy -m           -s         $versOpt wa >&wa.deplog &
#./deploy -m           -s         $versOpt wi >&wi.deplog &

wait
