name: Distcheck CI

on:
  push:
    branches: [ "devel", "master" ]

jobs:
# distcheck_matrix:
  distcheck:
#   strategy:
#     matrix:
#       os: [ubuntu-latest, macos-latest]
#       os: [macos-latest]
#   runs-on: ${{ matrix.os }}
#   runs-on: ${{ matrix.os }}
    runs-on: macos-latest
    steps:
#   - run: sudo apt install doxygen
    - run: sudo brew install doxygen
    - uses: actions/checkout@v3
      with:
        ref: ${{ github.ref_name }}
    - run: mkdir m4
    - run: autoreconf -fi
    - run: ./configure --disable-root-actions --with-noaaport
    - run: make all
    - run: make distcheck

