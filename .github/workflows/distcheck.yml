name: Distcheck CI

on:
  push:
#    branches: [ "devel", "master" ]
    branches: [ "*" ]

jobs:
# distcheck_matrix:
  distcheck:
#   strategy:
#     matrix:
#       os: [ubuntu-latest, macos-latest]
#       os: [macos-latest]
#   runs-on: ${{ matrix.os }}
#   runs-on: ${{ matrix.os }}
    runs-on: macos-latest
    steps:
#   - run: sudo apt install doxygen
    - run: brew install autoconf@2.69
    - run: export PATH="/usr/local/opt/autoconf@2.69/bin:$PATH"
    - run: echo $PATH
    - run: /usr/local/opt/autoconf@2.69/bin/autoreconf --version
    - run: brew install automake
    - run: brew install doxygen
    - uses: actions/checkout@v3
      with:
        ref: ${{ github.ref_name }}
    - run: mkdir m4
    - run: /usr/local/opt/autoconf@2.69/bin/autoreconf -fi
#    - run: ./configure --disable-root-actions --with-noaaport CPPFLAGS='-UTIOCEXCL -D__APPLE__' || cat config.log
    - run: ./configure --disable-root-actions --with-noaaport || cat config.log
    - run: make all
    - run: make distcheck